# Estos archivos corren en Linux: deben conservar finales de linea LF aunque
# se editen en Windows. Un retorno de carro de mas rompe bash y systemd.
*.sh text eol=lf
*.service text eol=lf
*.py text eol=lf
